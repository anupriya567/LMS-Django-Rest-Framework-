from django.urls import path,include
from . import views
from rest_framework.routers import DefaultRouter



# router = DefaultRouter()
# router.register('',views.DoubtViewSet,basename = 'doubts')

# # /api/reviews/
urlpatterns = [
 
#  path('doubts/',views.DoubtsByCourse.as_view(),name = "doubts-by-course"),
#  path('', include(router.urls)),

#  path('course/<str:course>/create/',views.ReviewCreateOnCourse.as_view(),name = "reviews-create")
 
]
